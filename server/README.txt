How to run


TODOs
Don't hold the whole getObject buffer in memory (low priority)

dockerize (halfway done, need to replace the config.json when creating the dockerfile so have the proper db configs)
dockerize UI

FIX gif type names. I kind of hate them
FIX the typing on the GifQuery. rn nullable keys, implemented as (a | undefined) values

create some indexes

Make UI pretty
Add search by tag
Add tags to Gif interface

pagination
